# 260817-backs

- source: done/280808-redesign.md, section "BACKS"
- rules speak backs/aids; sync speaks parents/cids

# Insight

- post-N: merges add no time, so every protocol fact except
  merge topology lives in the signed action files
- backs are Merkle-deep: aid hashes the file, whose backs are
  aids -> an action hash-chains its ENTIRE action ancestry
- two holes hashing cannot close (harmless while backs are
  read-only: list dag, get metadata):
    - envelope consistency: an author can sign a lie --
      backs that do not match the commit's parents
    - existence: a backs aid can dangle (not in this chain)
- NEVER flips: replay order, consensus contribution ranges,
  octopus/hardfork -- merge topology is envelope-only

# B7: pin backs to the envelope (do first)

- `commit()` checks `act.backs == ACTION.backs(ps)`
    - resolution already computed at write time
    - one array equality at replay; sorted both sides
    - error: `malformed commit : invalid backs`
    - place: with the envelope checks, BEFORE the dup skip
      (a dup in a tampered wrapper must still fail)
- crafted tests must carry REAL backs (a first attempt was
  reverted; redo notes):
    - err-post/err-like: capture the legit post aid, pass to
      the craft helpers
    - sync.lua step 7: tip may be a merge -> helper `BACKS`
      in tests.lua (mirror of ACTION.backs via diff-tree +
      rev-list, sorted, serialized)
    - crafts erroring BEFORE B7 (junk, forged path, extra
      file, bad kind) need nothing

# B8 DONE: action-level now

- `NOW(G, backs)` (rules.lua, global): fold entries' `now`
- entry gains `now = max(env.time, up)` (posts AND votes)
- apply: skew check = `NOW(G, act.backs)`; env sheds
  `parents` (writers + commit() updated) -- envelope-free
- commit() snapshot: action -> `G.actions[aid].now`;
  merge -> `NOW(G, ACTION.backs(ps))`
- sync final merge: same fold over HEAD^1/HEAD^2
- DIED: `GIT.time` (+ its memo), `STATE.peaks`
- snapshots still per-cid for sync (G_oct at octopus)
- snapshot format: entries carry `now` (old chains
  unsupported, as before)
