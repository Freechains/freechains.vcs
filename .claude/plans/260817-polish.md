# 260817-polish

- source: done/280808-redesign.md ("Constant names" + flagged
  strings + doc work)
- user-visible naming and docs; wire-invisible

# Docs

- README.md: refresh transcript (aids, raw units, `abandon`,
  neutral dates -- `git log` no longer meaningful)
- guide.sh: keep runnable end to end after refresh

# R: constant renames (constants.lua)

- R1: `C.fork.posts` -> `C.fork.actions`
    - counts `G.order` entries, and order takes any kind
    - call sites: sync.lua `hardfork` (x2)
- R2: `C.like.tax`/`C.like.split` -> `C.vote.tax`/`C.vote.split`
    - rules.lua uses them for like AND revoke
- R3: `C.time.diff` comment says "max post time diff":
  applies to EVERY kind -> "action"; name `skew` open

- checked, correct as-is: `time.half/full`, `reps.cost`,
  `reps.max`, `fork.time`, `reps.earn`, `reps.revoke`,
  `post.size`

# Error strings still saying "hash"

- `chain reps : action requires a hash` -> aid
- `chain reps : revoke requires a hash` -> aid
- `chain abandon : invalid hash` (x2) -> aid
- chains.lua `local hash = "#" .. gen`: dirname, not a git
  id -> rename var (`name`)
