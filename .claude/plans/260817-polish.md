# 260817-polish

- source: done/280808-redesign.md ("Constant names" + flagged
  strings + doc work)
- user-visible naming and docs; wire-invisible

# Docs

- DONE: README.md API list: `list ...` (dag, order, begs,
  revokes); `abandon` drops an action and all after it
- DONE: README.md "most identifiers depend on local creation
  time" (aids embed `time`; chain id embeds the nonce)
- won't do: `get metadata` comments (already accurate)
- won't do: `~aid~` revoked markers in the transcript
- won't do: rename "unique identifier" -> "action id"
- pending: guide.sh runnable end to end after refresh

# R DONE: constant renames (constants.lua)

- R1 DONE: `C.fork.posts` -> `C.fork.actions`
    - counts `G.order` entries, and order takes any kind
    - call sites: sync.lua `hardfork` (x2) + its header comment
- R2 DONE: `C.like.*` -> `C.vote.*` (whole table renamed)
    - rules.lua uses them for like AND revoke
- R3 DONE: `C.time.diff` comment now says "action"
    - name `skew` still open (won't do?)

- checked, correct as-is: `time.half/full`, `reps.cost`,
  `reps.max`, `fork.time`, `reps.earn`, `reps.revoke`,
  `post.size`

# DONE: error strings still saying "hash"

- DONE: `chain reps : action|revoke requires id`
- DONE: `chain abandon : invalid action` (x2)
    - expectations updated in cli-reps, cli-revoke, cli-abandon
- DONE: chains.lua `local hash` -> `id` (x2 blocks)
- user-visible strings say "action", never "aid" (jargon)
    - code keeps `aid` internally
- open: test names/comments still say "hash" (won't do?)
    - `reps-post-requires-hash`, `reps-revoke-no-hash`,
      `unknown hash rejects`, cli-abandon header
