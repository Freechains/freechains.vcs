# Plan: Complete README — Consensus section

## Context

`README.md` is a runnable guide (Alice / Bob / Charlie, peers A / B)
with real command outputs.
Its two trailing sections are empty commented-out stubs:
`### Consensus` and `### Censorship`.

Decision (this round):
- `### Consensus`: write a SIMPLE runnable example now.
- `### Censorship`: POSTPONE — its revocation feature is unimplemented
  (`reps.md` TODO, Rule 3.b).

## Implemented behavior (verified against code)

Fork resolution lives in `src/freechains/chain/sync.lua`:
- `consensus()` (`sync.lua:78-112`) picks the winning branch by the
  summed reputation of its signers at the common ancestor; ties break
  by smaller commit hash (`sync.lua:107`).
- On a divergent `sync recv`, the winner's posts are ordered first and
  the loser's new posts are appended (`sync.lua:258`, `354-410`).
- `list order` then shows winner-first; `list dag` shows a 2-parent
  merge.

Do NOT document (not implemented / contradicts code):
- 7-day / 100-post hard fork (`consensus.md`, NOT IMPLEMENTED).
- "earlier author date wins" wording (`consensus.md:17`) — the code is
  reputation-first.

## Approach: `### Consensus`

Replace the commented stub (`README.md` ~315-333) with a runnable
example that extends the existing setup (peer A's daemon already
running; Bob already has reps from the Reputation section).

Command sequence (user runs to capture REAL hashes / outputs):

1. Alice posts on peer A:
   `freechains chain '#chat' post inline $'Alice was here\n' --sign=/tmp/alice`
2. Bob posts on peer B — no sync between, this is the divergence:
   `freechains --root=/tmp/B/ chain '#chat' post inline $'Bob was here\n' --sign=/tmp/bob`
3. Peer B receives A's branch, triggering resolution:
   `freechains --root=/tmp/B/ chain '#chat' sync recv localhost`
4. Consensus — Alice (more reps) ordered first:
   `freechains --root=/tmp/B/ chain '#chat' list order`
5. The merge in the DAG (two parents):
   `freechains --root=/tmp/B/ chain '#chat' list dag`

Plus one short prose paragraph stating the rule: the branch with more
reputation in the common prefix wins (ties by commit hash) — realizing
the README headline "consensus via authoring reputation".

Reuse: existing peer-A-daemon + `--root=/tmp/B/` pattern; Alice > Bob
in reps at the common ancestor, so the winner is deterministic and
explainable (no new setup needed).

## `### Censorship`: postpone

Leave the heading + commented stub unchanged (revocation unimplemented).

## Files

| file        | place                       | change                                   |
|-------------|-----------------------------|------------------------------------------|
| `README.md` | `### Consensus` (~315-333)  | replace stub with runnable example + prose |

## Verification

- User runs the 5-command sequence end-to-end against a fresh `/tmp/B/`
  clone with A's daemon up; confirm `list order` shows Alice's post
  before Bob's, and `list dag` shows a 2-parent merge.
- Paste the real outputs into the section (replacing placeholders).
- `make` still passes (README is untested, but confirms `src` untouched).

## Pending

- [x] Decide: Consensus runnable; Censorship postponed
- [ ] Draft + insert `### Consensus` section
- [ ] User runs the sequence, fills real outputs
- [ ] (later) `### Censorship` once revocation ships
- [ ] (optional) flag `consensus.md` "earlier-date-wins" contradiction
